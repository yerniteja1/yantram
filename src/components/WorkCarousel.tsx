// Removed: the 3D Works carousel was replaced by a lightweight horizontal
// scroll-snap row in src/app/page.tsx for better performance.
// This stub exists only so the file tree stays explicit — safe to delete
// this file entirely.
export {};
